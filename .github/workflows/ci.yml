name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  shared-foundation:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Validate shared foundation
        run: ./scripts/validate.sh
